defaults:
  - /watch
  - _self_

seed: 123

emulator:
  baseline_state_path: local/states/first-race.state
  renderer: angrylion

env:
  reset_to_race: true
  action:
    name: steer_drive_boost_drift

watch:
  fps: 30
