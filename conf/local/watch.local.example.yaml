defaults:
  - /watch
  - _self_

seed: 123

emulator:
  baseline_state_path: checkpoints/baselines/mute-city-novice-blue-falcon-balanced-go-window.state
  renderer: angrylion

env:
  reset_to_race: true
  camera_setting: close_behind
  action:
    name: steer_drive_boost_lean

watch:
  control_fps: auto
  render_fps: 60
  deterministic_policy: true
