defaults:
  - /train
  - _self_

seed: 123

emulator:
  baseline_state_path: local/states/first-race.state
  renderer: angrylion

train:
  vec_env: dummy
  output_root: local/runs
  run_name: ppo_cnn
